@page
@model PraiseUS.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Avis[0].note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avis[0].commentaire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avis[0].avisDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Avis) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.commentaire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.avisDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.avisId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.avisId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.avisId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
